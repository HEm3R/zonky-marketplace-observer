rootProject.name = "zonky-marketplace-observer"

val info = "${rootProject.name} (java ${JavaVersion.current()})"

println()
println("+-${"-".repeat(info.length)}-+")
println("| $info |")
println("+-${"-".repeat(info.length)}-+")
